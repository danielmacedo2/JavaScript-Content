function sum(a, b) {
    var operation = a + b;
    console.log('A + B é igual a ' + operation)
}

function multiplication(a, b) {
    var op = a * b;
    console.log(`${a} X ${b} = ${op}`)
}

sum(2, 5);

multiplication(5, 10)